// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
	"gopkg.in/nullbio/null.v5"
)

// TextAnnotation is an object representing the database table.
type TextAnnotation struct {
	TextID      string      `boil:"text_id" json:"text_id" toml:"text_id" yaml:"text_id"`
	PageID      null.String `boil:"page_id" json:"page_id,omitempty" toml:"page_id" yaml:"page_id,omitempty"`
	Description string      `boil:"description" json:"description" toml:"description" yaml:"description"`
	Locale      string      `boil:"locale" json:"locale" toml:"locale" yaml:"locale"`
	Xcomin      float64     `boil:"xcomin" json:"xcomin" toml:"xcomin" yaml:"xcomin"`
	Xcomax      float64     `boil:"xcomax" json:"xcomax" toml:"xcomax" yaml:"xcomax"`
	Ycomin      float64     `boil:"ycomin" json:"ycomin" toml:"ycomin" yaml:"ycomin"`
	Ycomax      float64     `boil:"ycomax" json:"ycomax" toml:"ycomax" yaml:"ycomax"`

	R *textAnnotationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L textAnnotationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// textAnnotationR is where relationships are stored.
type textAnnotationR struct {
	Page *Page
}

// textAnnotationL is where Load methods for each relationship are stored.
type textAnnotationL struct{}

var (
	textAnnotationColumns               = []string{"text_id", "page_id", "description", "locale", "xcomin", "xcomax", "ycomin", "ycomax"}
	textAnnotationColumnsWithoutDefault = []string{"page_id", "description", "locale", "xcomin", "xcomax", "ycomin", "ycomax"}
	textAnnotationColumnsWithDefault    = []string{"text_id"}
	textAnnotationPrimaryKeyColumns     = []string{"text_id"}
)

type (
	// TextAnnotationSlice is an alias for a slice of pointers to TextAnnotation.
	// This should generally be used opposed to []TextAnnotation.
	TextAnnotationSlice []*TextAnnotation
	// TextAnnotationHook is the signature for custom TextAnnotation hook methods
	TextAnnotationHook func(boil.Executor, *TextAnnotation) error

	textAnnotationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	textAnnotationType                 = reflect.TypeOf(&TextAnnotation{})
	textAnnotationMapping              = queries.MakeStructMapping(textAnnotationType)
	textAnnotationPrimaryKeyMapping, _ = queries.BindMapping(textAnnotationType, textAnnotationMapping, textAnnotationPrimaryKeyColumns)
	textAnnotationInsertCacheMut       sync.RWMutex
	textAnnotationInsertCache          = make(map[string]insertCache)
	textAnnotationUpdateCacheMut       sync.RWMutex
	textAnnotationUpdateCache          = make(map[string]updateCache)
	textAnnotationUpsertCacheMut       sync.RWMutex
	textAnnotationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var textAnnotationBeforeInsertHooks []TextAnnotationHook
var textAnnotationBeforeUpdateHooks []TextAnnotationHook
var textAnnotationBeforeDeleteHooks []TextAnnotationHook
var textAnnotationBeforeUpsertHooks []TextAnnotationHook

var textAnnotationAfterInsertHooks []TextAnnotationHook
var textAnnotationAfterSelectHooks []TextAnnotationHook
var textAnnotationAfterUpdateHooks []TextAnnotationHook
var textAnnotationAfterDeleteHooks []TextAnnotationHook
var textAnnotationAfterUpsertHooks []TextAnnotationHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TextAnnotation) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TextAnnotation) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TextAnnotation) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TextAnnotation) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TextAnnotation) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TextAnnotation) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TextAnnotation) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TextAnnotation) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TextAnnotation) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range textAnnotationAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTextAnnotationHook registers your hook function for all future operations.
func AddTextAnnotationHook(hookPoint boil.HookPoint, textAnnotationHook TextAnnotationHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		textAnnotationBeforeInsertHooks = append(textAnnotationBeforeInsertHooks, textAnnotationHook)
	case boil.BeforeUpdateHook:
		textAnnotationBeforeUpdateHooks = append(textAnnotationBeforeUpdateHooks, textAnnotationHook)
	case boil.BeforeDeleteHook:
		textAnnotationBeforeDeleteHooks = append(textAnnotationBeforeDeleteHooks, textAnnotationHook)
	case boil.BeforeUpsertHook:
		textAnnotationBeforeUpsertHooks = append(textAnnotationBeforeUpsertHooks, textAnnotationHook)
	case boil.AfterInsertHook:
		textAnnotationAfterInsertHooks = append(textAnnotationAfterInsertHooks, textAnnotationHook)
	case boil.AfterSelectHook:
		textAnnotationAfterSelectHooks = append(textAnnotationAfterSelectHooks, textAnnotationHook)
	case boil.AfterUpdateHook:
		textAnnotationAfterUpdateHooks = append(textAnnotationAfterUpdateHooks, textAnnotationHook)
	case boil.AfterDeleteHook:
		textAnnotationAfterDeleteHooks = append(textAnnotationAfterDeleteHooks, textAnnotationHook)
	case boil.AfterUpsertHook:
		textAnnotationAfterUpsertHooks = append(textAnnotationAfterUpsertHooks, textAnnotationHook)
	}
}

// OneP returns a single textAnnotation record from the query, and panics on error.
func (q textAnnotationQuery) OneP() *TextAnnotation {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single textAnnotation record from the query.
func (q textAnnotationQuery) One() (*TextAnnotation, error) {
	o := &TextAnnotation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for text_annotation")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all TextAnnotation records from the query, and panics on error.
func (q textAnnotationQuery) AllP() TextAnnotationSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TextAnnotation records from the query.
func (q textAnnotationQuery) All() (TextAnnotationSlice, error) {
	var o TextAnnotationSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TextAnnotation slice")
	}

	if len(textAnnotationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all TextAnnotation records in the query, and panics on error.
func (q textAnnotationQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TextAnnotation records in the query.
func (q textAnnotationQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count text_annotation rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q textAnnotationQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q textAnnotationQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if text_annotation exists")
	}

	return count > 0, nil
}

// PageG pointed to by the foreign key.
func (o *TextAnnotation) PageG(mods ...qm.QueryMod) pageQuery {
	return o.Page(boil.GetDB(), mods...)
}

// Page pointed to by the foreign key.
func (o *TextAnnotation) Page(exec boil.Executor, mods ...qm.QueryMod) pageQuery {
	queryMods := []qm.QueryMod{
		qm.Where("page_id=?", o.PageID),
	}

	queryMods = append(queryMods, mods...)

	query := Pages(exec, queryMods...)
	queries.SetFrom(query.Query, "\"page\"")

	return query
}

// LoadPage allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (textAnnotationL) LoadPage(e boil.Executor, singular bool, maybeTextAnnotation interface{}) error {
	var slice []*TextAnnotation
	var object *TextAnnotation

	count := 1
	if singular {
		object = maybeTextAnnotation.(*TextAnnotation)
	} else {
		slice = *maybeTextAnnotation.(*TextAnnotationSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &textAnnotationR{}
		}
		args[0] = object.PageID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &textAnnotationR{}
			}
			args[i] = obj.PageID
		}
	}

	query := fmt.Sprintf(
		"select * from \"page\" where \"page_id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Page")
	}
	defer results.Close()

	var resultSlice []*Page
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Page")
	}

	if len(textAnnotationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Page = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PageID.String == foreign.PageID {
				local.R.Page = foreign
				break
			}
		}
	}

	return nil
}

// SetPageG of the text_annotation to the related item.
// Sets o.R.Page to related.
// Adds o to related.R.TextAnnotations.
// Uses the global database handle.
func (o *TextAnnotation) SetPageG(insert bool, related *Page) error {
	return o.SetPage(boil.GetDB(), insert, related)
}

// SetPageP of the text_annotation to the related item.
// Sets o.R.Page to related.
// Adds o to related.R.TextAnnotations.
// Panics on error.
func (o *TextAnnotation) SetPageP(exec boil.Executor, insert bool, related *Page) {
	if err := o.SetPage(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetPageGP of the text_annotation to the related item.
// Sets o.R.Page to related.
// Adds o to related.R.TextAnnotations.
// Uses the global database handle and panics on error.
func (o *TextAnnotation) SetPageGP(insert bool, related *Page) {
	if err := o.SetPage(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetPage of the text_annotation to the related item.
// Sets o.R.Page to related.
// Adds o to related.R.TextAnnotations.
func (o *TextAnnotation) SetPage(exec boil.Executor, insert bool, related *Page) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"text_annotation\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"page_id"}),
		strmangle.WhereClause("\"", "\"", 2, textAnnotationPrimaryKeyColumns),
	)
	values := []interface{}{related.PageID, o.TextID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PageID.String = related.PageID
	o.PageID.Valid = true

	if o.R == nil {
		o.R = &textAnnotationR{
			Page: related,
		}
	} else {
		o.R.Page = related
	}

	if related.R == nil {
		related.R = &pageR{
			TextAnnotations: TextAnnotationSlice{o},
		}
	} else {
		related.R.TextAnnotations = append(related.R.TextAnnotations, o)
	}

	return nil
}

// RemovePageG relationship.
// Sets o.R.Page to nil.
// Removes o from all passed in related items' relationships struct (Optional).
// Uses the global database handle.
func (o *TextAnnotation) RemovePageG(related *Page) error {
	return o.RemovePage(boil.GetDB(), related)
}

// RemovePageP relationship.
// Sets o.R.Page to nil.
// Removes o from all passed in related items' relationships struct (Optional).
// Panics on error.
func (o *TextAnnotation) RemovePageP(exec boil.Executor, related *Page) {
	if err := o.RemovePage(exec, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemovePageGP relationship.
// Sets o.R.Page to nil.
// Removes o from all passed in related items' relationships struct (Optional).
// Uses the global database handle and panics on error.
func (o *TextAnnotation) RemovePageGP(related *Page) {
	if err := o.RemovePage(boil.GetDB(), related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemovePage relationship.
// Sets o.R.Page to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *TextAnnotation) RemovePage(exec boil.Executor, related *Page) error {
	var err error

	o.PageID.Valid = false
	if err = o.Update(exec, "page_id"); err != nil {
		o.PageID.Valid = true
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Page = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.TextAnnotations {
		if o.PageID.String != ri.PageID.String {
			continue
		}

		ln := len(related.R.TextAnnotations)
		if ln > 1 && i < ln-1 {
			related.R.TextAnnotations[i] = related.R.TextAnnotations[ln-1]
		}
		related.R.TextAnnotations = related.R.TextAnnotations[:ln-1]
		break
	}
	return nil
}

// TextAnnotationsG retrieves all records.
func TextAnnotationsG(mods ...qm.QueryMod) textAnnotationQuery {
	return TextAnnotations(boil.GetDB(), mods...)
}

// TextAnnotations retrieves all the records using an executor.
func TextAnnotations(exec boil.Executor, mods ...qm.QueryMod) textAnnotationQuery {
	mods = append(mods, qm.From("\"text_annotation\""))
	return textAnnotationQuery{NewQuery(exec, mods...)}
}

// FindTextAnnotationG retrieves a single record by ID.
func FindTextAnnotationG(textID string, selectCols ...string) (*TextAnnotation, error) {
	return FindTextAnnotation(boil.GetDB(), textID, selectCols...)
}

// FindTextAnnotationGP retrieves a single record by ID, and panics on error.
func FindTextAnnotationGP(textID string, selectCols ...string) *TextAnnotation {
	retobj, err := FindTextAnnotation(boil.GetDB(), textID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTextAnnotation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTextAnnotation(exec boil.Executor, textID string, selectCols ...string) (*TextAnnotation, error) {
	textAnnotationObj := &TextAnnotation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"text_annotation\" where \"text_id\"=$1", sel,
	)

	q := queries.Raw(exec, query, textID)

	err := q.Bind(textAnnotationObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from text_annotation")
	}

	return textAnnotationObj, nil
}

// FindTextAnnotationP retrieves a single record by ID with an executor, and panics on error.
func FindTextAnnotationP(exec boil.Executor, textID string, selectCols ...string) *TextAnnotation {
	retobj, err := FindTextAnnotation(exec, textID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TextAnnotation) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TextAnnotation) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TextAnnotation) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *TextAnnotation) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no text_annotation provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(textAnnotationColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	textAnnotationInsertCacheMut.RLock()
	cache, cached := textAnnotationInsertCache[key]
	textAnnotationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			textAnnotationColumns,
			textAnnotationColumnsWithDefault,
			textAnnotationColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(textAnnotationType, textAnnotationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(textAnnotationType, textAnnotationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"text_annotation\" (\"%s\") VALUES (%s)", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"text_annotation\" DEFAULT VALUES"
		}

		if len(cache.retMapping) != 0 {
			cache.query += fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into text_annotation")
	}

	if !cached {
		textAnnotationInsertCacheMut.Lock()
		textAnnotationInsertCache[key] = cache
		textAnnotationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single TextAnnotation record. See Update for
// whitelist behavior description.
func (o *TextAnnotation) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single TextAnnotation record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *TextAnnotation) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the TextAnnotation, and panics on error.
// See Update for whitelist behavior description.
func (o *TextAnnotation) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the TextAnnotation.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *TextAnnotation) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	textAnnotationUpdateCacheMut.RLock()
	cache, cached := textAnnotationUpdateCache[key]
	textAnnotationUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(textAnnotationColumns, textAnnotationPrimaryKeyColumns, whitelist)
		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update text_annotation, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"text_annotation\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, textAnnotationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(textAnnotationType, textAnnotationMapping, append(wl, textAnnotationPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update text_annotation row")
	}

	if !cached {
		textAnnotationUpdateCacheMut.Lock()
		textAnnotationUpdateCache[key] = cache
		textAnnotationUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q textAnnotationQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q textAnnotationQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for text_annotation")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TextAnnotationSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TextAnnotationSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TextAnnotationSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TextAnnotationSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), textAnnotationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"UPDATE \"text_annotation\" SET %s WHERE (\"text_id\") IN (%s)",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(textAnnotationPrimaryKeyColumns), len(colNames)+1, len(textAnnotationPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in textAnnotation slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TextAnnotation) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TextAnnotation) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TextAnnotation) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *TextAnnotation) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no text_annotation provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(textAnnotationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	textAnnotationUpsertCacheMut.RLock()
	cache, cached := textAnnotationUpsertCache[key]
	textAnnotationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			textAnnotationColumns,
			textAnnotationColumnsWithDefault,
			textAnnotationColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			textAnnotationColumns,
			textAnnotationPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert text_annotation, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(textAnnotationPrimaryKeyColumns))
			copy(conflict, textAnnotationPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"text_annotation\"", updateOnConflict, ret, update, conflict, whitelist)

		cache.valueMapping, err = queries.BindMapping(textAnnotationType, textAnnotationMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(textAnnotationType, textAnnotationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert text_annotation")
	}

	if !cached {
		textAnnotationUpsertCacheMut.Lock()
		textAnnotationUpsertCache[key] = cache
		textAnnotationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single TextAnnotation record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TextAnnotation) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single TextAnnotation record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TextAnnotation) DeleteG() error {
	if o == nil {
		return errors.New("models: no TextAnnotation provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single TextAnnotation record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TextAnnotation) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single TextAnnotation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TextAnnotation) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no TextAnnotation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), textAnnotationPrimaryKeyMapping)
	sql := "DELETE FROM \"text_annotation\" WHERE \"text_id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from text_annotation")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q textAnnotationQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q textAnnotationQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no textAnnotationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from text_annotation")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TextAnnotationSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o TextAnnotationSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no TextAnnotation slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TextAnnotationSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TextAnnotationSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no TextAnnotation slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(textAnnotationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), textAnnotationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"DELETE FROM \"text_annotation\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, textAnnotationPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(textAnnotationPrimaryKeyColumns), 1, len(textAnnotationPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from textAnnotation slice")
	}

	if len(textAnnotationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TextAnnotation) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TextAnnotation) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TextAnnotation) ReloadG() error {
	if o == nil {
		return errors.New("models: no TextAnnotation provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TextAnnotation) Reload(exec boil.Executor) error {
	ret, err := FindTextAnnotation(exec, o.TextID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TextAnnotationSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TextAnnotationSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TextAnnotationSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty TextAnnotationSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TextAnnotationSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	textAnnotations := TextAnnotationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), textAnnotationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"SELECT \"text_annotation\".* FROM \"text_annotation\" WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, textAnnotationPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(textAnnotationPrimaryKeyColumns), 1, len(textAnnotationPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&textAnnotations)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TextAnnotationSlice")
	}

	*o = textAnnotations

	return nil
}

// TextAnnotationExists checks if the TextAnnotation row exists.
func TextAnnotationExists(exec boil.Executor, textID string) (bool, error) {
	var exists bool

	sql := "select exists(select 1 from \"text_annotation\" where \"text_id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, textID)
	}

	row := exec.QueryRow(sql, textID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if text_annotation exists")
	}

	return exists, nil
}

// TextAnnotationExistsG checks if the TextAnnotation row exists.
func TextAnnotationExistsG(textID string) (bool, error) {
	return TextAnnotationExists(boil.GetDB(), textID)
}

// TextAnnotationExistsGP checks if the TextAnnotation row exists. Panics on error.
func TextAnnotationExistsGP(textID string) bool {
	e, err := TextAnnotationExists(boil.GetDB(), textID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TextAnnotationExistsP checks if the TextAnnotation row exists. Panics on error.
func TextAnnotationExistsP(exec boil.Executor, textID string) bool {
	e, err := TextAnnotationExists(exec, textID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
